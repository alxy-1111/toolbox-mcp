{
  "name": "toolbox-mcp",
  "version": "2.0.0",
  "description": "前端实用工具箱 MCP 服务器(TypeScript)",
  "type": "module",
  "main": "dist/server.js",
  "bin": {
    "toolbox-mcp": "dist/server.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "inspect": "npm run build && npx @modelcontextprotocol/inspector node dist/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
